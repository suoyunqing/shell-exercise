#!/usr/bin/env bash
# #!用来声明脚本由什么shell程序执行，否则使用默认shell

echo "shell creation"

#三种执行方式：
# ./shell-script-creation-1.sh,使用脚本指定的shell程序。如果指定的不存在， 则使用默认shell程序；
# bash shell-script-creation-1.sh,指明先使用bash，若不存在，再使用脚本指定的shell程序；
# source shell-script-creation-1.sh,直接使用默认的shell程序。

#chmod +x shell-script-creation-1.sh, 添加执行权限；
